{
  "name": "speech_analysis",
  "schema": {
    "type": "object",
    "properties": {
      "intent": {
        "type": "string",
        "description": "intento principal del cliente, como 'consulta de saldo', 'reclamo', 'solicitud de información', etc."
      },
      "account_id": {
        "type": "string",
        "description": "Número de cuenta del cliente si se menciona"
      },
      "product": {
        "type": "string",
        "description": "Producto al que se refiere la conversación"
      },
      "sentiment": {
        "type": "string",
        "enum": ["positivo", "neutral", "negativo"]
      },
      "urgency": {
        "type": "integer",
        "description": "Nivel de urgencia percibido en la conversación, de 1 a 5",
        "minimum": 1,
        "maximum": 5
      }
    },
    "required": ["intent"]
  }
}