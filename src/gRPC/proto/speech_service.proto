syntax = "proto3";

package speech;

service SpeechService {
  rpc Transcribe (stream TranscribeRequest) returns (TranscribeResponse);
  rpc Analyse (stream AnalyseRequest) returns (AnalyseResponse);
  rpc Ask (AskRequest) returns (AskResponse);
}

message TranscribeRequest {
    bytes audio = 1;
    string filename = 2;
}

message TranscribeResponse {
    string text = 1;
}

message AnalyseRequest {
    bytes audio = 1;
    string filename = 2;
}

message AnalyseResponse {
    string result = 1;
}

message AskRequest {
    string prompt = 1;
}

message AskResponse {
    string response = 1;
}